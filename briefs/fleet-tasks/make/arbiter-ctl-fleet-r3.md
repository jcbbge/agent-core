# ARBITER brief — ctl-fleet TASKS Verify Q (nQ round 3/3 — LAST)

Exactly ONE ruling. Do not fix.

## State
Retest r2: 6 pass / 1 fail (A2 only).
`.done`: `~/agent-core/briefs/fleet-tasks/.done/tester-ctl-fleet-r2.done`
Log: `/tmp/tester-ctl-fleet-r2-run.log`

A2: expects `ORCH fleet-task-cli  ◐ Drafting the CLI core`
actual: `ORCH fleet-task  ◐ Drafting the CLI core`

r2 arbiter already pre-verified: plan golden uses unit **title**; `unitLabel()`
prefers owner_agent slug. This is the queued coder route.

nQ budget: this is round 3/3. If you cannot rule cleanly, escalate to human
via board note `to: CORD fleet-tasks —`. Prefer BAD IMPLEMENTATION → coder if
evidence holds.

Board finding from `AGNT arbiter-ctl-fleet-r3`.
`.done`: `~/agent-core/briefs/fleet-tasks/.done/arbiter-ctl-fleet-r3.done`
