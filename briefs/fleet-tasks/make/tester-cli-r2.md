# Tester brief — fleet-task CLI Verify (re-run after nQ1)

Do NOT use emojis anywhere.

You are the **tester**. RUN only; never edit. After test-maker nQ1 fixes.

## Tasks

1. Run: `cd ~/agent-core/primitives/tools/fleet-task && bun test fleet-task.test.ts`
2. Report pass/fail with full output. If fail, list failing names; hand Q to arbiter.
3. `.done`: `~/agent-core/briefs/fleet-tasks/.done/tester-cli-r2.done`
4. Board finding on `agent-core/fleet-tasks`.

Do not commit. Touch nothing under fleet-task product/test sources.
