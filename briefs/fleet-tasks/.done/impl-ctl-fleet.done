# ORCH ctl-fleet-tasks — U3 DONE

unit: impl-ctl-fleet (TASKS section in ctl-fleet)
orch: ORCH ctl-fleet-tasks (w2H:p3)
verify: cursor-fleet make → tester/arbiter nQ 3/3 → GREEN automated
human-QA: test/qa/ctl-fleet-tasks-human.md (A6 unticked — CORD/operator)
uncommitted: yes (CORD gates commits)

## Per-file diff summary

| path | change |
|------|--------|
| `bin/ctl-fleet` | +TASKS store reader (`loadTasksStore`/`FLEET_TASKS_STORE` via `loadJson` in `refreshSlow`), `renderTasksMachine`/`renderTasksProject` above WORK, glyphs ○◐✓⊘, collapse, `CTL_FLEET_DUMP`/`--dump` seam; `unitLabel` prefers `unit.title` |
| `docs/ctl-fleet.md` | Short TASKS paragraph (store path, silence, cost model, project filter) |
| `test/ctl-fleet-tasks.sh` | Acceptance oracle A1–A5 (no mocks; dump + fixtures) |
| `test/fixtures/fleet-tasks/two-projects.json` | Two-project fixture (all four statuses, collapse, active_form, CORD+ORCH) |
| `test/fixtures/fleet-tasks/invalid.json` | Unparseable store |
| `test/fixtures/fleet-tasks/malformed-only.json` | Missions missing progress/rollup |
| `test/qa/ctl-fleet-tasks-human.md` | A6 human-QA checklist (class=human) |

Sample render artifact: `briefs/fleet-tasks/.done/ctl-fleet-tasks-sample-render.txt`

## Test command + tail

```
cd ~/herdr-spine && bash test/ctl-fleet-tasks.sh
```

```
PASS: A4: fixtures only under test/fixtures/fleet-tasks (no live store paths)
PASS: A5: tasks store I/O confined to refreshSlow; render path is cache-only
PASS: A1a: missing store → no TASKS, exit 0
PASS: A1b: unparseable JSON → no TASKS, exit 0
PASS: A1c: malformed-only store → no TASKS, exit 0
PASS: A2: machine-plane TASKS shape, glyphs, collapse, active_form, progress counts
PASS: A3: project plane filters to basename(project) only

Results: 7 passed, 0 failed
```

(Official: `.done/tester-ctl-fleet-r3.done` — exit 0, 7/0)

## Sample TASKS render (fixture)

```
TASKS
  agent-core  m-fleet-tasks  ✓2 ◐1 ○3 ⊘0
    ORCH fleet-task-cli  ◐ Drafting the CLI core
      ✓ Schema pinned
      ◐ Draft the CLI core
      ○ Board + .done
    ORCH Schema rollout  ✓ Schema rollout
  herdr-spine  m-spine-ctl  ✓1 ◐0 ○1 ⊘1
    ORCH spike-retired  ⊘ spike-retired
    CORD fleet-tasks-cord  ○ fleet-tasks-cord
      ✓ Review rollout
      ○ Hold verify gate

WORK
```

## Verify beat / nQ

1. BAD TEST — capture_dump rc lost in subshell + A5 awk range
2. BAD TEST — awk reserved var `in`
3. BAD IMPLEMENTATION — `unitLabel` used owner_agent slug; fixed to prefer title

## Deviations

- HEAD at start was `63e1010` (brief cited `bb307a4`) — no impact; path clean for ctl-fleet at claim.
- Added `CTL_FLEET_DUMP=1`/`--dump` one-shot seam (planned) for fixture tests without touching live CTRL `w29:p12`.
- No schema deviation from parallel ORCH fleet-task-cli on the board during Plan.
- A6 human-QA left unticked for CORD live chrome check.

## Not touched

`bin/handlers/`, `~/.fleet-tasks/`, `primitives/tools/fleet-task/`, live CTRL pane.
