# shim-nonrepo-wall — implementer done (w29-p2n)

worktree: /private/tmp/qa-nonrepo-dir.B783I7 (non-repo --dir spawn; no worktree fork)

partition: cursor-spine, cursor-fleet

diff summary:
- cursor-spine: `is_git_repo_dir()` helper; gate forced/explicit `--worktree` on `git -C "$DIR" rev-parse --git-dir`; log `isolation: <dir> is not a git repo — worktree N/A, profile-discipline fallback`
- cursor-fleet make: conditional `MAKE_WT_ARGS=(--worktree)` only when target dir is a git repo; in-place spawn + fallback log for non-repo dirs

evidence:
- AC1 dry-run: `--dir /private/tmp/qa-nonrepo-dir.B783I7` → `worktree : 0` + fallback line; cmd omits `--worktree`
- AC1 live: criteria gate PERMITTED + fallback logged (no launch failure)
- AC2 dry-run: `--dir <git-repo>` → `worktree : 1` + cmd carries `--worktree wt-agnt-coder-w29-p2n`
- verify gate unchanged: criteria-authored check still enforced on non-dry-run coder spawns

commit: none (per brief; CORD gates)
