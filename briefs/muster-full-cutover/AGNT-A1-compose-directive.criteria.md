# Criteria — AGNT-A1 compose-directive

1. `~/agent-core/primitives/agent-bridge/compose-directive concierge cursor '' cursor-concierge ''` exits 0.
2. That stdout contains `muster-deposit` and `muster-spawn`.
3. That stdout contains no `field.py`, no `~/tup/`, no `bellman`, no `spine-report`, no `herdr-spine`.
