# Criteria — AGNT desk-status-dictum

Gate for `/Users/jrg/agent-core/briefs/muster-full-cutover/AGNT-desk-status-dictum.md`. Coder may implement only after `muster-spawn verify-mark` records this file. Independent test path: rg + Read of the desk-card bullet. No mocks.

1. `rg -n 'under pressure|fold under pressure' /Users/jrg/agent-core/primitives/profiles/concierge.md` shows both strings (at least one hit containing `under pressure`, at least one hit containing `fold under pressure`).
2. Those hits sit inside the **Desk status** bullet of `## The desk card` (same bullet as `~/bin/desk-status`).
3. That same bullet still names `~/bin/desk-status`, forbids `python3 -c` for herdr JSON, forbids head/cat/Read of compiled binaries with `muster-deposit` as Mach-O example, forbids keep-going shell chains after a probe crash, and keeps does-not-apply = product Python doors (`muster-spawn`) and reading source `.lisp`/`.py`/`.md`.
4. Applies clause includes "under pressure" (desk house-state pulls, all harnesses, under pressure). The dictum "rise to preparation; do not fold under pressure" is present in that bullet (same bullet or the next sentence of it).
5. `primitives/skills/concierge/SKILL.md` is untouched by this agent (git/mtime vs this unit).
