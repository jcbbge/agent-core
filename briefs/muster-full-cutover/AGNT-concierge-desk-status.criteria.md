# Criteria — AGNT concierge desk-status

1. `rg -n 'desk-status' /Users/jrg/agent-core/primitives/profiles/concierge.md` hits inside `## The desk card`.
2. That bullet names `~/bin/desk-status`, forbids `python3 -c` for herdr JSON, forbids head/cat/Read of compiled binaries with `muster-deposit` as Mach-O example, forbids keep-going shell chains after a probe crash, states apply = concierge desk house-state pulls / all harnesses, does-not-apply = `muster-spawn` and reading source `.lisp`/`.py`/`.md`.
3. `rg -n 'desk-status' /Users/jrg/agent-core/primitives/skills/concierge/SKILL.md` is a pointer (not a second full copy of the ruling). Pending-mail gather remains.
