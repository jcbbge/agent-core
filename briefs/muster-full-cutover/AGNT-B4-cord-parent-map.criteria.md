# Criteria — AGNT-B4 cord parent map

1. `compose_directive_parent` returns `"cursor-concierge"` when `from_name` starts with `cord-`.
2. Function docstring says `cord-* → cursor-concierge`.
3. `compose-directive` file mtime unchanged.
4. Profile-name `concierge` hits at the role map / default profile / help text are untouched.
