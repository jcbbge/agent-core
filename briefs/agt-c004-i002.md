# Brief: AGT c004-i002 — build unblock, then "the side panel never swallows a key"
Date: 2026-08-10
Cycle: c004 · item i002 · Role: IMPLEMENTER

## What This Is
You are the implementer for c004 item i002, and you carry the cycle's
BUILD UNBLOCK as your first task. The branch does not compile right now;
a peer agent cannot run a single test until you fix that and commit. Do
task 1 and commit it BEFORE you start task 2.

You implement only. You do NOT write tests, and you do NOT run the full
suite — a separate Test-Author and Test-Runner own those (Isolation
Mandate). You DO verify your own work compiles and lints.

## Working location
- Worktree: `~/.herdr/worktrees/future/c004-ux` (branch `c004-ux`). Work here.
- ALL cargo commands need this toolchain (the shim misresolves):
  `PATH=~/.rustup/toolchains/1.95.0-aarch64-apple-darwin/bin:$PATH cargo ...`
- `.madewell/` does NOT exist in this worktree by design (it lives in the
  main checkout `/Users/jrg/future/.madewell/`). Do not create it here.

## YOUR FILE PARTITION — exclusive, and a hard boundary
- `src/client/mod.rs`
- `src/client/input.rs`

Nothing else. A peer is working IN THE SAME WORKTREE right now (see
Parallel Work). Never `git add -A` and never `git add .` — stage your
files by explicit path, every time.

## Pre-Verified Facts (verified by the orchestrator this hour — trust these)
- Branch `c004-ux` is checked out at commit `2e53f89` with the completed
  work of items i001 and i004 sitting UNCOMMITTED in the working tree,
  across 5 files: `src/client/mod.rs` (i001), and `src/protocol.rs`,
  `src/daemon/mod.rs`, `src/terminal/ghostty.rs`, `src/terminal/runtime.rs`
  (i004). That work is DONE and verified by its own implementers. Do not
  modify, revert, or "improve" any of it.
- The tree is RED with exactly ONE root cause: i004 added the variant
  `ServerMessage::ScrollbackSnapshot { terminal_id, screen }`
  (`src/protocol.rs:273`), which makes the exhaustive `match
  envelope.message` at `src/client/mod.rs:254` non-exhaustive. The compiler
  names the insertion point as immediately after this arm, at
  `src/client/mod.rs:578`:
  `ServerMessage::Welcome { .. } => bail!("unexpected second welcome from daemon"),`
  `cargo check --all-targets` reports this as TWO E0004 errors only because
  the lib and test targets each compile it — it is ONE code site. `cargo
  check --lib` reports exactly 1.
- i001 already landed the focus substrate you build on: `ChromeFocus::owns_pty()`
  (`src/client/mod.rs`, ~line 102) is THE single accessor for "who owns the
  keyboard". Use it; do not introduce a second notion of focus.
- The swallow site you must change is the `PrefixAction::Send(bytes)` arm
  inside `Event::Key(key) if surface.is_none()` (around `src/client/mod.rs:832`).
  It currently reads `if !chrome_focus.owns_pty() {` and its comment says
  outright that every non-ring bare key "is swallowed here". Ring gestures
  are handled inside via `ring_move(key)`.
- `commit_side_panel_focus(framed, &resources, &view, &phoropter, &mut focus)`
  ALREADY EXISTS and is what the ring-boundary transition calls when focus
  moves to the main panel (ruling Q1: the focus request fires ONCE, at the
  ring transition). Reuse it — do not write a second focus-commit path.
- `PrefixState::feed` (`src/client/input.rs:104`) returns `Wait` /
  `Dispatch(action)` / `Send(bytes)`; `Ctrl-b` is byte `[2]`. Prefix
  sequences must keep working exactly as they do today.

## TASK 1 — BUILD UNBLOCK (do this first, commit it, then continue)
Add the missing `ServerMessage::ScrollbackSnapshot` arm at
`src/client/mod.rs:578`.

- Write an EXPLICIT named arm. Do NOT use a wildcard `_ =>` — the whole
  reason this error was caught is that the match is exhaustive, and a
  wildcard would silently swallow every future variant. Do NOT use
  `todo!()`/`unimplemented!()` — that turns a compile error into a runtime
  panic.
- The correct behaviour TODAY is to ignore it with a comment: no client code
  sends `RequestScrollback` yet, so an unsolicited `ScrollbackSnapshot` is
  simply not actionable. Comment the arm to say that consuming it belongs to
  item i005, which owns the scroll UI.

Then make ONE commit. It must include i001's and i004's uncommitted files
alongside your arm, because a commit containing your arm WITHOUT i004's
`protocol.rs` would not compile — this is the integration commit that gives
the branch a green base, and you are explicitly authorized to make it:

    git add src/client/mod.rs src/protocol.rs src/daemon/mod.rs \
            src/terminal/ghostty.rs src/terminal/runtime.rs
    # verify you are committing i004's work UNCHANGED, and that no peer's
    # files (sidebar.rs, cards.rs, chrome.rs, config.rs) are staged:
    git diff --cached --stat

Stage those five paths and nothing else. Commit message body must use the
project's format (`PHASE:`/`DONE:`/`TODO:` — see the repo's CLAUDE.md).
Before committing, confirm green:
`cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`,
`cargo test --no-run`.

THE MOMENT that commit exists, post the board note in Report Back step 2 —
a peer agent is blocked on it.

## TASK 2 — i002: the side panel must NEVER swallow a non-ring key
The operator's finding, verbatim mandate (d017 part 2, DECISIONS.md
2026-08-10): "do not deter from the user experience." You never type into
the void.

Any keypress that is NOT a focus-ring gesture (Tab/Shift+Tab) and NOT a dial
gesture (Return/Shift+Return) must, when the side panel holds focus:
1. snap focus to the main panel (the same committed transition the ring
   boundary already performs), AND
2. DELIVER that exact keystroke to the PTY — not dropped, not doubled, not
   reordered.

Constraints that are not yours to trade away:
- Ring keys (Tab/Shift+Tab) and dial keys (Return/Shift+Return) still act on
  the side panel and are NOT delivered to the PTY.
- `Ctrl-b` prefix sequences still reach the chrome, from either focus state.
- A bare `Tab` is NEVER intercepted while the main panel has focus (Tab is
  shell completion; ruling: breaking it product-wide is a far worse loss).
- Derive focus from `ChromeFocus::owns_pty()`. One source of truth.

Done means: with the side panel focused, typing an ordinary character moves
focus to the main panel and that exact character reaches the PTY.

## Parallel Work — read this before you touch anything
`AGT c004-i003` is working IN THE SAME WORKTREE, concurrently, on item i003
(focus indication). Its exclusive files are `src/client/sidebar.rs`,
`src/client/cards.rs`, `src/client/chrome.rs`, `src/client/config.rs`.
- Do not open those files for editing. Do not stage them.
- It is blocked on your Task 1 commit before it can compile or test
  anything. Speed on Task 1 is a courtesy that matters.
- If you believe you need a change in one of its files, STOP and report to
  the orchestrator (board topic `c004`) instead of reaching across.

## Standing rules paid for in blood last cycle
- Verify by the ARTIFACT, never by your own sense of having done it. Quote
  your own `git diff` in your report.
- After ANY change to a function signature or struct shape, verification
  means `cargo test --no-run` AND `cargo clippy --all-targets` — never the
  narrow `cargo build` + scoped clippy pair. A whole gate run was lost to
  exactly that shortcut.
- If a `#[allow(dead_code)]` becomes necessary, it needs a named payer and a
  due date, or it does not go in.

## Report back (exact contract)
Post to the fleet board, topic `c004`, `from` = `c004-impl-i002`:
1. FIRST, before editing: a CLAIM — `CLAIM i002 + build unblock — pane
   <your pane id> — owns src/client/mod.rs, src/client/input.rs`.
2. Immediately after the Task 1 commit lands: `UNBLOCKED — <commit sha> —
   branch compiles` plus the four verification exit codes. This one is
   time-critical; a peer is waiting.
3. At the end: `DONE i002` with — what you changed and where (file:line),
   your `git diff --stat`, the exact behaviour now on a non-ring key, how
   ring/dial/Ctrl-b remain unaffected, exit codes for `cargo fmt --check` /
   `cargo clippy --all-targets -- -D warnings` / `cargo test --no-run`, and
   anything you deliberately did NOT do.
Then stop. Do not run the full test suite; do not write tests.
