ORCH A2 — Ground-manifest exhaust producer (CC-strong): emit deps.json + coverage from grounding reads.

Repo: ~/agent-core. Hook live path today: ~/.claude/hooks/grounding-hook.mjs (NOT yet git-tracked under agent-core — verify and either adopt into primitives/hooks/ or document deploy mirror). Do NOT use emojis anywhere.

Mission: after A2 shape co-sign, implement the producer side — deps is exhaust from compulsory grounding reads, never hand-authored; coverage mandatory and honest about shell-lossy holes. Cursor exhaust is weak/out of scope for this unit's primary land (CC-strong fence).

## Pre-Verified Facts (CORD Tower verified 2026-08-14)

- Shape (CO-SIGNED): `/Users/jrg/agent-core/briefs/manifest-and-alarms/A2-GROUND-MANIFEST-SHAPE.md` — schema `ground-manifest/v1`; §5 matrix: absent/empty → fail visible (NOT refuse); stale ref/mtime → refuse; coverage holes → flag; malformed schema → refuse.
- Co-sign evidence: board id `cli-098a9c38-d9c7-4c57-9045-a7c885237e36` (cwd cursor-shim); Tower amended §5 and work-done field WA `ph-mss9lphb-mvpc`.
- Artifact path: `<project>/.madewell/ground/<cycle-or-unit-key>/deps.json`.
- Live CC hook: `~/.claude/hooks/grounding-hook.mjs` — PostToolUse `*` + PreToolUse write gate; state under `/tmp/cc-grounding`. Settings: `~/.claude/settings.json` PostToolUse matcher `*` runs `bun …/grounding-hook.mjs post`.
- Cursor has no PostToolUse — do not invent a "cursor-first" exhaust in this unit; optional stub/docs only.
- `/ground` as a Made Well door exists in madewell SPEC; no standalone `ground` CLI on PATH verified tonight. Exhaust rides the existing grounding-hook observation surface (+ any synthesis step you locate without inventing policy).
- Sibling A8 ORCH is live on `w2Y:p1V` touching `tower-ledger.mjs` / `lib.mjs` / `server.mjs` / COMMS-ARCH — **do not touch those files**.

## Parallel Work Notice

- ORCH a8-alarms owns ledger/question validation — ignore their uncommitted diffs.
- CORD cursor-shim owns A3 freshness gate consumer — do not edit ~/cursor-shim.
- Board/field topic: `tower/manifest-and-alarms`.

## Tower

- Claim WA if emitted; heartbeat; work-done with evidence (deps.json sample + test path + commit sha).
- Post findings as `ORCH a2-exhaust`. nQ=0 before work-done.

## File partition (you own)

1. Prefer canonicalizing `grounding-hook.mjs` under `~/agent-core/primitives/hooks/` if missing, with deploy note; or patch `~/.claude/hooks/grounding-hook.mjs` AND add the tracked copy — one source of truth, say which.
2. Exhaust log: on PostToolUse (and Bash best-effort path extract), append observed paths into session exhaust state; on demand / session boundary / explicit flush, write `deps.json` per schema in A2 shape (coverage.mode defaults `shell-lossy` when Bash used without extractable paths; `unaudited_shell_calls` must not silently zero).
3. Minimal docs: `primitives/mcps/tower/GROUND-MANIFEST.md` (or section pointer from COMMS-ARCH) describing producer contract matching co-signed shape.
4. Tests: pure functions for path extract + coverage accounting + schema shape; fixture proving unaudited_shell_calls increments.
5. Sample: emit one example `.madewell/ground/demo/deps.json` under briefs/manifest-and-alarms/fixtures/ (not a hand-authored lie — generated by the emitter against a recorded PostToolUse fixture).

Out of scope: A3 walker; A1 night orders; A8; cursor preToolUse exhaust implementation (document as follow-on owned with cursor-shim).

## Branch / land

- Branch `feat/a2-ground-manifest-exhaust` from main.
- Stage explicitly; commit with PHASE/DONE/TODO handoff.

## Done-when

1. Emitter produces schema-valid `deps.json` with mandatory `coverage`.
2. deps entries are observed exhaust only (test proves no hand-authored path injection API for agents).
3. Shell-lossy honesty: unaudited Bash increments coverage counters.
4. Docs match co-signed §5.
5. Tests green; board finding + `.done` under briefs/manifest-and-alarms/; field work-done if claimed.

## Report-back

Board finding on `tower/manifest-and-alarms` with sha, test counts, residual (cursor exhaust). Reap when CORD verifies.
