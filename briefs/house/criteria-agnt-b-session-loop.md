# Acceptance criteria — AGNT B session-loop (law encode)

Unit: encode failures 2 and 5 into control-flow §Reaping and ending-session.
Not product code — house law markdown only.

## Must pass (ORCH verifies against disk)

1. `primitives/rules/control-flow.md` §Reaping states Diagnosis ≠ Land; mission workspace close only when outer item is Landed or Parked on disk with a pickup brief; sub-phase `.done` does not authorize `workspace close`.
2. Same §Reaping (and/or ending-session) states reap-as-law: take a resource, return it at Done/Park (panes, worktrees, Arc Docker allowlist, Neon numeric allowlist).
3. `docker system prune -a` remains banned; Arc invariant 8 cited not edited.
4. `skills/ending-session/SKILL.md` Step 1 carries the Diagnosis≠Land / Park exception aligned with control-flow.
5. Prefer extending control-flow; `session-loop.md` only if needed and linked.
6. No edits outside the two exclusive files. No commit.
