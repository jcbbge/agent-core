# Acceptance criteria — AGNT C session-loop (law encode)

Unit: encode failures 1 (starting-session), 6 (skip-ritual), 7, and COMMS mailbox one-liner if missing.
Not product code — house law markdown only.

## Must pass (ORCH verifies against disk)

1. `skills/starting-session/SKILL.md` does not unconditionally ask "What are we expanding into?" when threads are already named or NOW/flight already names them.
2. Breathe-mode stop-and-wait remains only when the pool is empty (skip-ritual tightened for collect = named artifact).
3. `skills/brief/SKILL.md` does not instruct briefs to name provider/model/`--kind`; profiles/roles only; models via profile-model; spawn verbs in directives/<harness>.md.
4. `AGENTS.md` and `directives/cursor.md` tightened so a brief cannot violate encoding 7.
5. `profiles/coordinator.md` and `profiles/orchestrator.md` desk-card spawn lines drop hardcoded `--kind`; one line each inherit stop-states (d) and reap (e); never ask operator "are we done."
6. `mcps/tower/COMMS-ARCH.md` has mailbox ≠ substrate / PHASE2-WRITE-GATE sentence if it was missing.
7. Today's briefs under `briefs/` that still instruct opus/fable/sonnet/claude-as-kind for spawns are fixed forward (or finding: none needed).
8. No edits outside exclusive files (+ today brief fixes). No commit.
