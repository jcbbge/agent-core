# Brief: AGT c004-i003 — unmissable focus indication
Date: 2026-08-10
Cycle: c004 · item i003 · Role: IMPLEMENTER

## What This Is
You are the implementer for c004 item i003: make it obvious, at a glance,
where the keyboard is pointing. This is a direct operator mandate, not a
nice-to-have.

You implement only. You do NOT write tests, and you do NOT run the full
suite — a separate Test-Author and Test-Runner own those (Isolation
Mandate). You DO verify your own work compiles and lints.

## The mandate (DECISIONS.md 2026-08-10, d017 part 1)
The operator signed off c003 with two findings, one of them Tab-ring modal
confusion, and framed the follow-up verbatim: *"do not deter from the user
experience. i dont give a fuck what we do but we need a better user
experience."*

The HOW is delegated to you. The OUTCOME is not: at a glance, from across
the room, it must be obvious which region owns the keyboard.

Three required elements:
1. The focused region carries a loud border/highlight.
2. The unfocused side panel visibly DIMS.
3. The status line names the current mode in words.

## Working location
- Worktree: `~/.herdr/worktrees/future/c004-ux` (branch `c004-ux`). Work here.
- ALL cargo commands need this toolchain (the shim misresolves):
  `PATH=~/.rustup/toolchains/1.95.0-aarch64-apple-darwin/bin:$PATH cargo ...`
- `.madewell/` does NOT exist in this worktree by design (it lives in the
  main checkout `/Users/jrg/future/.madewell/`). Do not create it here.

## YOUR FILE PARTITION — exclusive, and a hard boundary
- `src/client/sidebar.rs`
- `src/client/cards.rs`
- `src/client/chrome.rs`
- `src/client/config.rs`

Nothing else. `config.rs` is YOURS EXCLUSIVELY for this cycle — a later item
(i005) was originally scoped to touch it too and has been deliberately kept
out so you can work without coordination. Never `git add -A` and never
`git add .` — stage your files by explicit path, every time.

## START HERE — you are blocked for a few minutes, and that is expected
The branch does NOT COMPILE at the moment you read this. This is known, it
is not your doing, and it is not something for you to fix.

Cause: item i004 added `ServerMessage::ScrollbackSnapshot` to
`src/protocol.rs`, which leaves the exhaustive match at
`src/client/mod.rs:254` non-exhaustive (E0004, insertion point line 578).
`src/client/mod.rs` belongs to `AGT c004-i002`, which is adding that arm
right now as its first task and committing immediately.

So: read and plan first, and watch for i002's `UNBLOCKED — <sha>` post on
board topic `c004` (or just poll `git log --oneline -1`). You can edit your
files meanwhile; you simply cannot get a clean compile until that lands. Do
NOT add the arm yourself, and do NOT touch `src/client/mod.rs`.

## Pre-Verified Facts (verified by the orchestrator this hour — trust these)
- Branch `c004-ux` sits at `2e53f89` with items i001 and i004 complete but
  UNCOMMITTED across 5 files (`src/client/mod.rs`, `src/protocol.rs`,
  `src/daemon/mod.rs`, `src/terminal/ghostty.rs`, `src/terminal/runtime.rs`).
  i002 will commit all of those together with its arm. Do not stage them
  yourself, and do not revert them.
- i001 landed the focus substrate: `ChromeFocus::owns_pty()`
  (`src/client/mod.rs`, ~line 102) is THE single accessor for "who owns the
  keyboard", and `ChromeFocus` has variants `MainPanel` and `SidePanel`.
  READ it for the shape, derive your rendering from that focus value, and do
  not invent a parallel notion of focus.
- `StylesConfig` is at `src/client/config.rs:257`, with `StylePatch` (270)
  and `StylesPatch` (296), a hand-written `Deserialize` (307) that patches
  over `Default` (332), and an `impl StylesConfig` (371). Its existing named
  styles are `normal`, `muted`, `current`, `selected`, `closing`,
  `attention`, `error`, `divider`. Config exposes it as
  `pub(super) styles: StylesConfig` (748). Any new style knob must go
  through this patch/default machinery so `[ui.styles.*]` overrides keep
  working — that is exactly what the done-condition means by "no hard-coded
  colour that ignores StylesConfig".
- File sizes for orientation: `chrome.rs` 2730 lines, `sidebar.rs` 1339,
  `cards.rs` 972, `config.rs` 1540. Navigate with grep, not full reads.
- Three icon presets and style overrides exist today and must all still
  work.

## Done means
- Rendered buffers differ UNMISTAKABLY between the side-panel-focused and
  main-panel-focused states, in BOTH the border/highlight AND the dim level.
- The status line names the mode in words.
- All three icon presets and style overrides still work.
- No hard-coded colour that ignores `StylesConfig`.

## Parallel Work — read this before you touch anything
`AGT c004-i002` is working IN THE SAME WORKTREE, concurrently, on item i002
(non-ring keys must never be swallowed). Its exclusive files are
`src/client/mod.rs` and `src/client/input.rs`.
- Do not open those files for editing. Do not stage them.
- It will make a commit that includes five files outside your partition.
  That is authorized. Keep your own staging explicit so your in-progress
  edits never ride along in its commit and vice versa.
- If you believe you need a change in `mod.rs` or `input.rs` — for instance
  if the status-line mode text has to be fed from the run loop — STOP and
  report to the orchestrator on board topic `c004`. Do not reach across.
  Naming that dependency early is cheap; a cross-partition edit is not.

## Standing rules paid for in blood last cycle
- Verify by the ARTIFACT, never by your own sense of having done it. Quote
  your own `git diff` in your report.
- After ANY change to a function signature or struct shape, verification
  means `cargo test --no-run` AND `cargo clippy --all-targets` — never the
  narrow `cargo build` + scoped clippy pair. A whole gate run was lost to
  exactly that shortcut.
- A lint left able to speak caught two shipping bugs last cycle. If a
  `#[allow(dead_code)]` becomes necessary, it needs a named payer and a due
  date, or it does not go in.
- You are rendering something a human will judge by eye. You do NOT
  self-certify the aesthetic: the coordinator eyeballs a live client and the
  operator signs at Land. Your job is that the difference is mechanically
  present and configurable.

## Report back (exact contract)
Post to the fleet board, topic `c004`, `from` = `c004-impl-i003`:
1. FIRST, before editing: a CLAIM — `CLAIM i003 focus indication — pane
   <your pane id> — owns src/client/sidebar.rs, src/client/cards.rs,
   src/client/chrome.rs, src/client/config.rs`.
2. At the end: `DONE i003` with — what you changed and where (file:line),
   your `git diff --stat`, the exact new style knobs and their defaults, how
   a reader can tell the two focus states apart in a rendered buffer (be
   concrete: which cell attributes differ), confirmation that the three icon
   presets and `[ui.styles.*]` overrides still work, exit codes for
   `cargo fmt --check` / `cargo clippy --all-targets -- -D warnings` /
   `cargo test --no-run`, and anything you deliberately did NOT do.
Then stop. Do not run the full test suite; do not write tests. Do not
commit i002's files.
