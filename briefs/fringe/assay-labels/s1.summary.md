# S1 Label Summary — agnt-assay-s1

Session: `2026-07-24T21-02-44-774Z` (circadian project audit / SELF.md accretion cure)

Wake payload: 50 `<mind:self>` atoms (2026-07-24 format: numbered Doctrine + Motif/How-we-work bullets; differs from current `claim — "quote"` template). Plus 5 decoys (all absent from wake).

## Counts (payload atoms, n=50)

| Label | Count |
|-------|-------|
| P3 SHAPED | 24 |
| P2 ECHOED | 15 |
| P0 INERT | 11 |

Decoy false-positives (P2/P3 on absent atoms): **0** / 5

Distinct claims with P3 (deduped): ~10 — cliff/accretion, load-bearing, fire/ash, Turn-End (×3 merged in distillation), compost, fail loudly, repo hygiene/no-mocks, terminal-native inspection (15 duplicate payload lines share one propagation event at L171).

## Three strongest P3 examples

1. **1. The cliff is complexity accretion** — line 229: `Circadian's entire reason to exist is Doctrine 1: complexity accretion killed five prior memory systems` → drove asymmetric-grammar diagnosis and catabolic verb design
2. **Fail loudly** — line 117: `"Fail loudly" requires the failure to say WHY` → drove `gitCommit` stderr-capture helper in `rem.ts`
3. **Repo hygiene: no mocks in tests** — line 436: `No mocks in tests is in your doctrine and I was still effectively mocking` → drove regression analysis on real episodes vs synthetic fixtures

## Three clearest P0 examples

1. **4. Nine disconnected memory organs needed vasculature, not a tenth organ** — no assistant trace
2. **7. Bidirectional state flow is the sole entry point to system work** — present in wake only
3. **Lake vs river: storage pools; memory must flow** — motif never referenced in assistant output

## Skeptic paragraph

A skeptic could argue several P3 labels reflect task-congruent narration rather than wake propagation: this session was explicitly an audit of circadian/SELF.md, so citing "complexity accretion" or "the cliff" may be inevitable given the user's question. The counter-evidence is mechanical — P3 atoms left line-numbered artifacts (MERGE mutations for Turn-End duplicates, `gitCommit` stderr capture for fail-loudly, `accretion.test.ts` pinning cliff grammar, test fixture redesign at L171 for terminal-native inspection). Those are decisions, not theme echoes. The high P3 row count (24/50) is inflated by 15 duplicate terminal-native lines in the wake payload that share one behavioral event; deduped P3 rate is ~10/35 unique claims (~29%). P0 rate (11/50) and 0/5 decoy false-positives suggest most injected bulk — especially stuttered How-we-work duplicates — was inert. Propagation is above coin-flip for this session but concentrated in doctrine atoms aligned with the dominant task.
