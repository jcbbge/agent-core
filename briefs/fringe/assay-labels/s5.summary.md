# agnt-assay-s5 — Label Summary

Session: `2026-08-11T18-32-09-386Z` (slim-build-w1b — Zig `slim` CLI implementation in agent-core)

## Counts

| | Present | Absent |
|---|---:|---:|
| **P3 SHAPED** | 0 | 0 |
| **P2 ECHOED** | 0 | 0 |
| **P0 INERT** | 52 | 5 |

- Payload atoms: 52 (all present in wake)
- Decoys: 5 (all absent; 0 false-positive propagations)

## Strongest P3 examples

None. No payload atom's own claim language appeared in assistant messages outside the circadian-wake injection (line 5). Session behavior was driven entirely by the impl-brief, spec.md, and rtk-clone fixtures — vocabulary like "truth over compactness", "byte-equal", "golden fixtures", and "ROLE slim-build-w1b REPORT" originates from the mission brief/spec, not from `<mind:self>` atom claims.

## Clearest P0 examples

1. **Motion is the metric — memory earns residence by causing thoughts.** — zero assistant trace; slim-build session never invoked this doctrine language.
2. **The user demands literal execution of instructions, and trust is earned through mechanical fidelity.** — assistant said "follow the brief exactly" (line 7) but that phrasing comes from the impl-brief contract, not the atom's "literal execution / mechanical fidelity" wording.
3. **Decoy D1 (literal, unadorned response to trivial input)** — absent from wake; no assistant propagation (correct negative control).

## Skeptic check

A skeptic would correctly conclude this session shows **no detectable atom propagation** above chance. The worker executed a highly specified mechanical build task where every behavioral driver is traceable to impl-brief.md and spec.md text echoed in assistant thinking blocks (line 7+). Distinctive atom phrases ("motion is the metric", "Ground truth is the run log", "Show, never describe", "mechanical fidelity", "pre-emptive verification") appear only in the circadian-wake custom_message, never in assistant output. The one superficial near-miss — "**ROLE slim-build-w1b REPORT:**" at line 308 — matches the brief's report-back contract, not atom #19's "exact structure / report line" language. Net: 0/52 present atoms shaped behavior by the strict attribution rule; decoy false-positive rate 0/5. This session is evidence *against* detectable propagation for a focused implementation worker.
