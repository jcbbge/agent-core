# ORCH unit tower/spine-board-flock — flock herdr-spine bare board_append

Do NOT use emojis anywhere. Repo: `/Users/jrg/herdr-spine` (git). Stack: Python 3 stdlib + bash (`spine-claim`). You are spawned via `cursor-fleet make` into an assigned worktree — **cwd is the edit root**. Do not edit the main checkout tree.

**Model tier:** sonnet-equivalent (mechanical flock + small unit test; no design fork left open).

## Pre-Verified Facts (ORCH verified 2026-08-14 this session)

1. Residual cites herdr-spine bare BOARD appends at `briefs/tower/bus-data/RESIDUAL-APPEND-SCAN.md` lines 50–53 (paths below).
2. Live bare writer `_spine_common.board_append` at `bin/handlers/_spine_common.py:326-340`:
   - builds entry dict (`id`, `ts`, `cwd`, `type`, `from`, `topic`, `body`)
   - `os.makedirs(os.path.dirname(TOWER_BOARD_PATH), exist_ok=True)`
   - `with open(TOWER_BOARD_PATH, "a") as fh: fh.write(json.dumps(entry) + "\n")`
   - **no flock**
3. Live bare writer embedded in `bin/spine-claim` Python helper `board_append(path, …)` at lines 86–100: same open-append + `json.dumps + "\n"`; creates parent dirs when dirname non-empty; **no flock**. Bash wrapper calls `python3 "$PYHELPER" board_append "$BOARD_PATH" …` (lines ~149–150).
4. Canonical flocked contract (doctrine, do not edit this file): `~/agent-core/primitives/hooks/tower-ledger.mjs` `append()` — `flock(2) LOCK_EX` around stringify+write, newline-terminated JSON, mkdir parent. Python match: `fcntl.flock(fh.fileno(), fcntl.LOCK_EX)` for the write duration (UNLOCK on exit of `with` / close is fine if lock held for the write).
5. `TOWER_BOARD_PATH = os.environ.get("SPINE_BOARD_PATH") or ~/.tower/board.jsonl` in `_spine_common.py` (~line 32). `spine-claim` honors `SPINE_BOARD_PATH` the same way. Smoke/tests MUST set `SPINE_BOARD_PATH` to a temp file — never the live board.
6. Twin WA for cc-hooks/server.mjs is CLOSED — out of scope. cursor-shim printf WAs are NOT yours.
7. `import fcntl` works on this host (`fcntl.LOCK_EX == 2` verified). No existing `fcntl` usage under herdr-spine yet.
8. Existing handler oracle harness: `bin/handlers/tests/test_50_scent_digest.py` — plain `python3` unittest-style script (no pytest required). Prefer adding `bin/handlers/tests/test_board_append_flock.py` in that style.
9. Base: `main` at spawn time. Branch name **must** be `fix/spine-board-flock`.
10. Fork resolved by ORCH: **do not factor** spine-claim onto `_spine_common.board_append` (different signatures: env path vs explicit path arg; claim helper is self-contained). Apply identical fcntl LOCK_EX pattern at both sites.

## Parallel Work Notice

| Agent | Owns |
|-------|------|
| AGNT coder | `bin/handlers/_spine_common.py` (`board_append` only); `bin/spine-claim` (embedded `board_append` only). Create/use branch `fix/spine-board-flock`. |
| AGNT test-maker | `bin/handlers/tests/test_board_append_flock.py` (new). Derive from this plan only — do not read implementation. |

Ignore uncommitted dirty under herdr-spine (`.future/`, research/*, briefs/*, `bin/spine-wave`). Ignore agent-core / cursor-shim / Arc. Do not investigate, revert, or fix them.

## Tower

- Topic `tower/fully-operational` (primary). Short note on `tower/bus-data` when done.
- CLAIM on board before edits. Findings with specific paths/line proof. No doorbell. No `to:"operator"`.
- On Herdr: `spine-report task` / `spine-report verdict` optional; do not block on them.
- Do not hand-append to `board.jsonl` — use Tower MCP or `bun ~/.tower/cli.mjs post …`.

## Intent / acceptance (plan — both arms)

### A. Flock `_spine_common.board_append`
- Hold `fcntl.LOCK_EX` on the append file descriptor for the duration of the `json.dumps` write.
- Still newline-terminated JSON line.
- Still create parent directories.
- Still honor `SPINE_BOARD_PATH` / `TOWER_BOARD_PATH` (no entry-shape change).
- Prefer `with open(..., "a") as fh:` then `fcntl.flock(fh.fileno(), fcntl.LOCK_EX)` then write — lock released when fd closes.

### B. Flock `spine-claim` embedded `board_append`
- Same LOCK_EX contract on the explicit `path` argument.
- No bare `open(..., "a")` + write remains at either cited site.
- Done when: `rg -n 'open\(.*\"a\"\)' bin/handlers/_spine_common.py bin/spine-claim` shows flock between open and write (or equivalent locked write); ORCH will also verify no unlocked write path.

### C. Tests (test-maker — from this plan only)
Author `bin/handlers/tests/test_board_append_flock.py` that:
1. Imports / invokes `_spine_common.board_append` with `SPINE_BOARD_PATH` set to a temp path (create parent dir as needed).
2. Asserts: file exists, last byte is `\n`, last line `json.loads` succeeds, required keys present (`type`, `from`, `topic`, `body`, `id`, `ts`).
3. Concurrent writers: spawn ≥2 processes (subprocess or multiprocessing) each calling `board_append` many times against the same temp board; assert every line parses (no torn JSON) and line count equals total writes.
4. **NO MOCKS of `fcntl` / `flock`** — real fcntl only. Do not patch flock away.
5. Optionally exercise spine-claim's helper the same way if reachable without mocks; `_spine_common` coverage is mandatory.
6. Run style match `test_50_scent_digest.py` (`python3 path/to/test.py`, exit non-zero on fail).

### D. Smoke (coder, after impl — report only; tester will re-run)
```bash
TMP=$(mktemp /tmp/spine-board-flock.XXXXXX.jsonl)
SPINE_BOARD_PATH="$TMP" python3 -c '
from bin.handlers import ... 
# OR: importlib load of _spine_common.board_append
'
# Prefer: python3 -c with sys.path to handlers dir calling board_append("note","smoke","orch-test","tower/fully-operational")
# Then: prove file ends with newline + json.loads each line
```
Exact import path is the coder's local judgment; report the command used.

## Tasks (coder)

1. Ensure branch `fix/spine-board-flock` exists from current main tip in your worktree — done when: `git branch --show-current` is `fix/spine-board-flock` (or worktree branch naming that tracks it; do not push).
2. Flock `_spine_common.board_append` — done when: LOCK_EX held across the write; entry shape unchanged; SPINE_BOARD_PATH honored.
3. Flock spine-claim embedded `board_append` — done when: same contract; no unlocked append at that site.
4. Local smoke to temp `SPINE_BOARD_PATH` — done when: one write + newline + parseable JSON in the report.
5. Marker: `/Users/jrg/agent-core/briefs/tower/spine-board-flock/workers/coder.done` with paths changed + smoke command + output tail.
6. Do NOT commit. Do NOT merge. Do NOT push.

## Tasks (test-maker)

1. Author `bin/handlers/tests/test_board_append_flock.py` from Intent §C only — done when: file exists in your worktree; asserts cover single-write newline/JSON + concurrent no-tear; no fcntl mocks.
2. Marker: `/Users/jrg/agent-core/briefs/tower/spine-board-flock/workers/test-maker.done` with test path + how to run.
3. Do NOT read coder implementation. Do NOT commit. Do NOT push.

## Constraints

- Touch ONLY your partition under spawn cwd.
- Evidence markers under `/Users/jrg/agent-core/briefs/tower/spine-board-flock/workers/` allowed (create dir).
- No agent-core source edits. No cursor-shim. No `install.sh`. Do not delete `cc-hooks/`.
- Testing: no mocks of flock. Real fcntl.
- No commit (ORCH commits after Verify).

## Report back with

Per-file diff summary; `rg` proof around both `board_append` sites; smoke/test command + tail; list every file created/modified including markers; deviations with reasons.
