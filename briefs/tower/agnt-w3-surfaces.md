# AGNT [surfaces] — Prove every MCP tool + CLI verb

Read `/Users/jrg/agent-core/briefs/tower/w3-prove-planes-evidence/SHARED-PREFIX.md` first — it is the shared prefix. Everything below is your partition.

## Pre-Verified Facts (lead verified all of these personally)

See SHARED-PREFIX. CLI verbs: `status inbox board post emit field scan burn all projects`. MCP tools: fact 3. F9/F11 from audit. ORCH F9 pre-probe: both `board` and `board <topic>` returned 52 lines.

## Parallel Work Notice

See SHARED-PREFIX. Partition map:
- YOU write: `SURFACE.md`, `raw/surfaces/**`, `workers/surfaces.done`
- Ledger/Q&A deep proof is sibling's job — you may invoke ask/reply lightly only to fill the surface row ("invoked how / result"), then leave deep F1 matrix to sibling. Prefer non-overlapping probe ids (`from=AGNT w3-surfaces`).

## Tower

- CLAIM on `tower/w3-prove-planes` from=`AGNT w3-surfaces`.
- For `send_to_user`: prefer `kind=progress` for surface smoke; if you must probe deliverable/alert, mark body `W3-SURFACE-PROBE` and do not doorbell.
- STANDALONE board_post calls.

## Tasks

1. Build `/Users/jrg/agent-core/briefs/tower/w3-prove-planes-evidence/SURFACE.md` with one row per MCP tool (all 10) and one row per CLI verb (all 10). Columns: surface, invoked how (exact), result summary, pass/fail/gap, evidence pointer under `raw/surfaces/`.
2. Truncation — done when: document F11 / board verb truncation behavior with paste of truncated vs full if observable (or "not observed" with method).
3. `board <topic>` filter — done when: SURFACE.md row for `board` states pass/fail/gap on topic filtering with your own counts (coordinate with BOARD.md narrative; your row is authoritative for SURFACE checklist).
4. Do not land failing tests; if you notice a mechanical lock candidate, note it under "Test plan (not landed)" in SURFACE.md — ORCH owns task 7 gate with CORD.

## Constraints

- Touch ONLY: `.../SURFACE.md`, `raw/surfaces/**`, `workers/surfaces.done`.
- Absolute paths. No production edits. No commits.

## Report back with

- Count: N pass / N fail / N gap across 20 surfaces.
- Path to SURFACE.md.
- `workers/surfaces.done` after the table is complete for all 20.
