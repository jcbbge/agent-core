- **Claude Code:** `TodoWrite` for 3+ step tasks · MCP names
  `mcp__<server>__<tool>` · fleet =
  `spine-spawn … --kind claude --profile <name>[:option]` (`~/bin/spine-spawn` =
  `python3 ~/herdr-spine/bin/spine-spawn`; profiles:
  `~/agent-core/primitives/profiles/` + `profile-model`)
