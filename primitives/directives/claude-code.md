- **Claude Code:** `TodoWrite` for 3+ step tasks · MCP names
  `mcp__<server>__<tool>` · daily entry = `herdr claude` · fleet =
  `muster-spawn … --profile <name>[:option]` (kind from desk default unless
  `--kind`) (`~/bin/spine-spawn` forwarder to `~/muster/bin/muster-spawn`;
  profiles: `~/agent-core/primitives/profiles/` + `profile-model`) · fleet
  comms are the muster deposit door (`~/muster/bin/muster-deposit`); this
  harness invokes it via the shell; do not use a retired bus
