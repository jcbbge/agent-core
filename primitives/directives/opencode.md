- **opencode:** re-registered 2026-08-15 as the concierge's spawn seat for
  cursor fleets (profile-model rulings). Config dir `~/.config/opencode/`;
  composed entry `~/.config/opencode/AGENTS.md` (directive/core + this delta via
  `agent-core sync directive/core`). Skills under `~/.config/opencode/skills/`,
  agents under `~/.config/opencode/agent/`. Stack = herdr + muster + madewell;
  fleet comms through the muster deposit door `~/muster/bin/muster-deposit`.
